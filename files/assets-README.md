# Assets folder

Add these two files here to complete the site:

- `profile.jpg` — your photo, used on the About page avatar frame.
  Falls back to a stylized "SP" placeholder if missing.
- `resume.pdf` — your resume, linked from the "Download Resume" button
  on the About page.

Any reasonably sized image/PDF works — no special naming beyond these two filenames.
